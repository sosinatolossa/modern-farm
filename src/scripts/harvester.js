import {plantseeds} from "./tractor.js"

export const harvestPlants = (thePlantsArray) => {
    for (let index = 0; index < 3; index++) {
        for (let j = 0; j < 6; j++) {
            if (thePlantArray[index][j] === number) {
                console.log("checking number",number)
            }
        }
    }
}